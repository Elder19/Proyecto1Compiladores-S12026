empty ~ __main__ <| |>
|:
    int ~ x <- 5 !
    int ~ y <- 3 !
    int ~ z <- x + y !
    float ~ a <- 2.5 !
    float ~ b <- a * 3.0 !
    int ~ p <- 2 ^ 3 !
    int ~ m <- 10 % 3 !
    x <- x + 1 !
:|