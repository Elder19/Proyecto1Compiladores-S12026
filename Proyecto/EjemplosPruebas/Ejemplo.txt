empty ~ __main__ <| |>
|:
    int ~ x <- 5 !
    int ~ y <- 3 !
    int ~ z <- x + y !
    bool ~ a <- true !
    bool ~ b <- false !
    bool ~ r1 <- a # b !
    bool ~ r2 <- a @ b !
    bool ~ r3 <- $a !
    bool ~ r4 <- equal<|x, y|> !
    bool ~ r5 <- less_t<|x, y|> !
    x <- x + 1 !
    cout<|z|> !
    cout<|r1, r2|> !
    cin<|x|> !
    return ~ x !
:|